const initState = {
  initiatives: [
    { id: '1', title: 'title 1' },
    { id: '2', title: 'title 2' },
    { id: '3', title: 'title 3' }
  ]
};

const initiativeReducer = (state = initState, action) => {
  return state;
};

export default initiativeReducer;
